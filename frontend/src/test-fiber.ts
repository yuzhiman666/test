// 测试 @react-three/fiber 的导出是否正常
import { Group } from 'three'; // Group 是 Three.js 原生类
import { Canvas } from '@react-three/fiber'; // Canvas 来自 @react-three/fiber

// 验证代码（保持不变）
console.log('Canvas:', Canvas);
console.log('Group:', Group);